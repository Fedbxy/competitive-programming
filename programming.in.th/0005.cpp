#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    double a,b;cin>>a>>b;
    cout<<fixed<<setprecision(6)<<sqrt(pow(a,2)+pow(b,2))<<'\n';
    return 0;
}