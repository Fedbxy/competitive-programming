#include <bits/stdc++.h>

using namespace std;

int main(){
    ios::sync_with_stdio(false); cin.tie(0);

    double a,b,c;
    cin>>a>>b;
    c=sqrt(a*a+b*b);
    cout<<fixed<<setprecision(6)<<c<<endl;

    return 0;
}